/* app.overlay */

&spi1 {
    status = "okay";

    cs-gpios = <&gpioa 9 GPIO_ACTIVE_LOW>;

    ads1263: ads1263@0 {

        compatible = "ti,ads1263";

        reg = <0>;

        spi-max-frequency = <8000000>;

        drdy-gpios = <&gpioc 12 GPIO_ACTIVE_LOW>;

        reset-gpios = <&gpioa 10 GPIO_ACTIVE_HIGH>;

        #io-channel-cells = <1>;
    };
};